# Qt SDK (cpak)

## Installation

```bash
cpak install github.com/containerpak/sdk-qt
```

The package exports the Qt 6 build tools and includes the Core, Widgets and WebEngine development files.

Open the SDK shell, then run the build tools:

```bash
cpak shell github.com/containerpak/sdk-qt
qmake6 && make
```
